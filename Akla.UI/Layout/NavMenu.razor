<div class="page">
    <!-- Sidebar -->
    <div class="sidebar @(collapseNavMenu ? "collapsed" : "")">
        <div class="sidebar-header">
            <a class="navbar-brand fw-bold text-warning" href="">
                <i class="fas fa-utensils me-2"></i> <span>Akla</span>
            </a>
            <button class="btn-toggle" @onclick="ToggleNavMenu">
                <i class="fas @(collapseNavMenu ? "fa-bars" : "fa-times")"></i>
            </button>
        </div>

        <nav class="nav flex-column mt-4">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <i class="fas fa-home"></i> <span>Home</span>
            </NavLink>
            <NavLink class="nav-link" href="menu">
                <i class="fas fa-book-open"></i> <span>Menu</span>
            </NavLink>
            <NavLink class="nav-link" href="order">
                <i class="fas fa-shopping-cart"></i> <span>Order</span>
            </NavLink>
            <NavLink class="nav-link" href="reservation">
                <i class="fas fa-calendar-check"></i> <span>Reservations</span>
            </NavLink>
            <NavLink class="nav-link" href="about">
                <i class="fas fa-info-circle"></i> <span>About</span>
            </NavLink>
        </nav>
    </div>
</div>

@code {
    private bool collapseNavMenu = true;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
